  {{> _header2}}
<div class="container">
  <form action="/account/temp" method="post">
    <div class="row">
      <div class="col-sm-9 col-md-7 col-lg-5 mx-auto">
        <div class="card card-signin my-5">
          <div class="card-body">
            <h5 class="card-title text-center">Register</h5>
            <form class="form-signin">
              <button class="btn btn-lg btn-primary btn-block text-uppercase" value="s" name="which" type="submit" onclick="do(this.value)">Student Register</button>
              <button class="btn btn-lg btn-primary btn-block text-uppercase" value="c" name="which" type="submit" onclick="do(this.value)">College Register</button>

            </form>
          </div>
        </div>
      </div>
    </div>
    </form>
  </div>